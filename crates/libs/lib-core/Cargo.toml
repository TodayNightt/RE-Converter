[package]
name = "lib-core"
version = "0.1.0"
edition = "2021"

[dependencies]
# Local deps
lib-sorter = { workspace = true }
lib-utils = { workspace = true }

# External deps
tokio = { workspace = true, features = ["full"] }

serde = { version = "1", features = ["derive"] }
typeshare = "1.0.0"
